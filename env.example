import React from "react";
import { BookOpen, HelpCircle, Layers, ClipboardCheck, AlertCircle, FileText, ArrowUpRight } from "lucide-react";
import { motion } from "motion/react";

export default function Features() {

  const features = [
    {
      icon: <BookOpen className="h-5 w-5 text-indigo-500" />,
      title: "AI Summaries with Outline Guides",
      description: "Our algorithm synthesizes messy lecture transcriptions or dense textbooks into beautifully structured markdown outlines and term glossaries.",
      badge: "Structure",
      className: "md:col-span-2 lg:col-span-3"
    },
    {
      icon: <HelpCircle className="h-5 w-5 text-emerald-500" />,
      title: "Smart Adaptive Quizzes",
      description: "Auto-generates custom multiple-choice quizzes that measure real understanding with extensive correct-answer explanations after every action.",
      badge: "Testing",
      className: "md:col-span-2 lg:col-span-3"
    },
    {
      icon: <Layers className="h-5 w-5 text-indigo-500" />,
      title: "Active Flashcard decks",
      description: "Flippable digitised flashcards tracking your learning progress. Mark terms as memorized to filter them automatically under Leitner system schedules.",
      badge: "Recall",
      className: "md:col-span-2 lg:col-span-4"
    },
    {
      icon: <ClipboardCheck className="h-5 w-5 text-amber-500" />,
      title: "Situational Exam Mode",
      description: "Tackle high-impact open-ended scenarios resembling real university midterms, accompanied by customized evaluation rubrics.",
      badge: "Evaluation",
      className: "md:col-span-2 lg:col-span-4"
    },
    {
      icon: <AlertCircle className="h-5 w-5 text-rose-500" />,
      title: "Weakness Topic Detection",
      description: "Calvent logs questions missed during quiz playthroughs, flagging key weak areas with diagnostic reviews and curated focus recommendation guidelines.",
      badge: "Analytics",
      className: "md:col-span-4 lg:col-span-4"
    }
  ];

  return (
    <section id="features-section" className="py-20 bg-zinc-50 border-y border-zinc-100">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        
        {/* Section Header */}
        <div className="max-w-3xl mx-auto text-center mb-16 space-y-4">
          <span className="text-xs font-bold text-indigo-600 uppercase tracking-widest font-sans bg-indigo-50 px-3 py-1 rounded-full border border-indigo-100">
            Aesthetic and Ivy-League Grade Core Setup
          </span>
          <h2 className="font-display text-3xl sm:text-4xl font-bold tracking-tight text-zinc-900">
            Designed for heavy-weight academic execution.
          </h2>
          <p className="font-sans text-sm sm:text-base text-zinc-500 leading-relaxed">
            Avoid colorful childish layouts and messy flashcards. Calvent brings the pixel-perfect aesthetic of premium productivity software like Linear to higher education.
          </p>
        </div>

        {/* Bento Grid */}
        <div className="grid grid-cols-1 md:grid-cols-4 lg:grid-cols-12 gap-6">
          {features.map((feat, index) => (
            <motion.div
              key={index}
              whileHover={{ y: -3 }}
              transition={{ duration: 0.15 }}
              className={`p-6 bg-white border border-zinc-200/90 rounded-2xl flex flex-col justify-between study-card-border glow-subtle hover:border-zinc-300 transition-all ${feat.className}`}
              id={`feature-card-${index}`}
            >
              <div className="space-y-4">
                {/* Header Row */}
                <div className="flex items-center justify-between">
                  <div className="h-10 w-10 rounded-lg bg-zinc-50 flex items-center justify-center border border-zinc-100">
                    {feat.icon}
                  </div>
                  <span className="text-[10px] font-mono font-bold tracking-wider uppercase text-zinc-400 bg-zinc-100 px-2 py-0.5 rounded-full">
                    {feat.badge}
                  </span>
                </div>

                {/* Body Row */}
                <div className="space-y-2">
                  <h3 className="font-display font-bold text-base text-zinc-900 flex items-center gap-1">
                    {feat.title}
                  </h3>
                  <p className="font-sans text-xs sm:text-sm text-zinc-500 leading-relaxed font-normal">
                    {feat.description}
                  </p>
                </div>
              </div>

              {/* Action Indicator */}
              <div className="flex items-center gap-1 text-[11px] font-bold text-indigo-600 font-sans mt-4 pt-4 border-t border-zinc-50 hover:text-indigo-800 transition-colors cursor-pointer w-fit">
                Learn how it works
                <ArrowUpRight className="h-3 w-3" />
              </div>
            </motion.div>
          ))}
        </div>

      </div>
    </section>
  );
}
